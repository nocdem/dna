# Final HTTPS draft; the named certificate must exist before activation.
server {
    listen 80;
    listen [::]:80;
    server_name wiki.nodusnetwork.io;
    root /var/www/wiki.nodusnetwork.io/public;
    location ^~ /.well-known/acme-challenge/ {
        default_type text/plain;
        try_files $uri =404;
    }
    location / { return 301 https://wiki.nodusnetwork.io$request_uri; }
}
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    ssl_certificate /etc/letsencrypt/live/wiki.nodusnetwork.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wiki.nodusnetwork.io/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    server_name wiki.nodusnetwork.io;
    root /var/www/wiki.nodusnetwork.io/public;
    index index.html;
    autoindex off;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy no-referrer always;
    add_header Cache-Control "public, max-age=0, must-revalidate, no-transform" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self'; font-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'; form-action 'none'" always;
    location ^~ /.well-known/acme-challenge/ {
        default_type text/plain;
        try_files $uri =404;
    }
    location ~ /\. { return 404; }
    location / {
        limit_except GET { deny all; }
        try_files $uri $uri/ =404;
    }
}
